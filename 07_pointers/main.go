package main

func main() {

	// A pointer is a variable that stores the memory address of another variable. Instead of holding a value directly, it "points" to where the value is stored in memory, allowing you to access or modify the original data indirectly.

	// var ptr *int
	// fmt.Println("Value of pointer is ", ptr) // they show nil

	// myNumber := 44
	// var ptr = &myNumber // for reference use &

	// // fmt.Println("Value of actual pointer address is ", ptr)
	// // fmt.Println("Value of actual pointer value is ", *ptr)

	// *ptr = *ptr + 2
	// fmt.Println("New value is ", myNumber)

}
